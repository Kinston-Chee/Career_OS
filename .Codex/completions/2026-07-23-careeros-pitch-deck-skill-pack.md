# CareerOS pitch-deck skill pack

Created four project-local skills under `skills/`: strategy, slide design, screenshot curation, and deck QA. Each contains a focused `SKILL.md` and generated `agents/openai.yaml`. No deck was created or modified.

Sources inspected: the two July 22 analysis files, `src/App.jsx`, `package.json`, and the stale `pitch-deck/HANDOFF.md`.

Validation: run `quick_validate.py` against all four folders and verify the local PowerPoint render/inspection toolchain.
