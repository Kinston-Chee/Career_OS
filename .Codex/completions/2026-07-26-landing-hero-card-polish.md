# Landing hero and card polish

- Fixed encoded hero headline spacing that rendered visible `A` artifacts.
- Made landing card grids stretch to equal heights where rows should align.
- Added breathing room inside the large workspace and CTA cards so glow treatments stay away from borders.
- Verified with `npm.cmd run build`.
